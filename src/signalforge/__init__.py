"""SignalForge — AI-powered operations investigation.

Phase 1 provides the technical backbone: a deterministic synthetic operations
environment, a read-only MCP server exposing it, lexical retrieval over
runbooks and historical incidents, a client-side evidence registry, and a
thin wrapper around the official MCP client.
"""

__version__ = "0.1.0"
